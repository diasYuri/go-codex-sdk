# Samples

Executable examples for the Go SDK.

- `basic_streaming/`: iterate over streamed events
- `structured_output/`: request JSON structured output
- `resume_thread/`: continue an existing thread by ID
